{...}: {
  programs.starship = {
    enable = true;

    settings = {
      add_newline = true;

      # format = ''
      
      # '';

      # character = {
      #   success_symbol = null;
        
      # };
    };
  };
}
